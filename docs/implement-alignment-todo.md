# Implement alignment to-do

- [ ] Move **Loop turns on headland** from a tractor-only preference to an implement-level preference, so it follows the implement between tractors. Requested 13 September 2026. Define how multiple attached implements combine their requirements and retain a vehicle fallback for unconfigured implements. Update settings/profile storage, attachment changes and the UI together; test swapping tractors and attaching multiple implements. This is a future task, not part of the current bench setting.
